/**
 * Standalone deploy script for the ZK Consent Gateway contract.
 *
 * Usage:
 *   npm run build && npm run deploy:consent
 *
 * Prompts for a hex seed (or generates a new one), builds a wallet,
 * waits for sync + funds + dust, deploys the consent contract,
 * and writes deployment-consent.json with the contract address.
 */
export {};
